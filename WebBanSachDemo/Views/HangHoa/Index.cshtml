@using X.PagedList.Mvc.Core;
@model X.PagedList.IPagedList<WebBanSachDemo.ViewModels.HangHoaVM>
@{
    ViewData["Title"] = "Index";
    Layout = "_DanhSachHangHoa";
}
@section BreadCum{
    <div class="container-fluid page-header py-5">
        <h1 class="text-center text-white display-6">Trang chủ</h1>
        <ol class="breadcrumb justify-content-center mb-0">
            <li class="breadcrumb-item text-white">Trang Chủ</li>
            <li class="breadcrumb-item text-white">Dịch Vụ Tiện Ích</li>
        </ol>
    </div>
}

@* @foreach (var items in Model)
{
    <div class="col-md-6 col-lg-6 col-xl-4">
        <a asp-route-hinhanh="@items.MaHH">
            <div class="rounded position-relative fruite-item">
                <div class="fruite-img">
                    <img src="@items.Hinh" class="img-fluid w-100 rounded-top" alt="Mô tả về hình ảnh" style="width: 100%; height: 100%; object-fit: cover;">

                </div>
                <div class="text-white bg-secondary px-3 py-1 rounded position-absolute" style="top: 10px; left: 10px;">@items.TenLoai</div>
                <div class="p-4 border border-secondary border-top-0 rounded-bottom">
                    <p id="tenHH" style="font-size:20px;font-weight:bold;color:#1d2115; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 200px;">@items.TenHH</p>


                    <p style="font-size:15px; color:#1d2115">@items.MoTaNgan</p>
                    <div class="d-flex justify-content-between flex-lg-wrap">
                        <p class="text-dark fs-5 fw-bold mb-0">@string.Format("{0:0,0} VNĐ", @items.DonGia)</p>
                        <a href="#" class="btn border border-secondary px-3 text-primary"><i class="fa fa-shopping-bag me-2 text-primary"></i>Thêm vào giỏ hàng</a>
                    </div>
                </div>
            </div>
        </a>
    </div>
} *@
@await Html.PartialAsync("ProductItem" , Model)
<div class="col-12">
    <div class=" justify-content-center">
        <div class="row">
            <ul class="pagination"><div class="col-6" style="text-align:right;margin-top:30px"> @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))</div></ul>
            </div>
    </div>
</div>